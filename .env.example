NODE_ENV=development
HOST=127.0.0.1
PORT=8787
DATABASE_PATH=/data/sua-learning.db
BACKUP_DIR=/data/backups
APP_ORIGIN=http://127.0.0.1:5173

# Generate with: openssl rand -hex 32
SETUP_SECRET=
# Generate with: openssl rand -hex 32
SESSION_PEPPER=
# Generate base64 of exactly 32 random bytes without printing or logging the value: openssl rand -base64 32
LLM_ENCRYPTION_KEY=

SESSION_DAYS=14
TIME_ZONE=Asia/Seoul
